classbook.factory('userSetter', ['$cookieStore',
    function($cookieStore) {
        return {
            set: function(user) {
                // if ($cookieStore.get('currentUser.number') === user.number)
            }
        };
    }
]);